{
    "name": "biblioteca/sistema-gerenciamento",
    "description": "Sistema de Gerenciamento de Biblioteca",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Pablo Juan Tadini Soto"
        },
        {
            "name": "Vinícius Istchuk Volpato"
        }
    ],
    "require": {
        "php": ">=8.0",
        "vlucas/phpdotenv": "^5.6"
    },
    "require-dev": {
        "symfony/var-dumper": "^6.4"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ]
    }
}
